import React, { Component } from "react";
import PropTypes from "prop-types";
import { Form, Radio, Notify, Button } from "zent";

class Wallet extends Component {
  render() {
    return <div>Wallet Page</div>;
  }
}

export default Wallet;
